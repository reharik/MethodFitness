/**
 * Created by rharik on 6/10/15.
 */

require('must');


describe('appendToStreamPromiseTester', function() {
    before(function(){
        var temp = require('../../registry');
    });

    beforeEach(function(){
    });

    context('append to stream', ()=> {
        it('should throw error if no stream provided', ()=>{
        })
    });
});

